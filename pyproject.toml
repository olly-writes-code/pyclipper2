[project]
name = "pyclipper2"
dynamic = ['version']

[build-system]
requires = ['meson-python']

build-backend = 'mesonpy'

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]

[tool.pytest.ini_options]
testpaths = [
    "tests",
]
